const variabile = ['primo', 'secondo', 'terzo', 'quarto', 'quinto', 'sesto', 'settimo'];

/*
for(let i = variabile.length - 1; i >= 0; i--) {
    console.log(variabile[i]);
}
*/

variabile.push('ottavo');

console.log(variabile);